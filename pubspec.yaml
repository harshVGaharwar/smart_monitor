name: smart_monitor
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.7.1

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  http: ^1.6.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  excel: ^4.0.6

flutter:
  uses-material-design: true
